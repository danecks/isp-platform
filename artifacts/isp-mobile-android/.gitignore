# Capacitor / web bundle copiado
www/
node_modules/
dist/

# Android nativo (generado por `npx cap add android` localmente)
android/.gradle/
android/.idea/
android/build/
android/app/build/
android/local.properties
android/captures/
android/app/release/
android/app/debug/
android/gradle/wrapper/gradle-wrapper.jar

# Keystore — NUNCA commitear
*.jks
*.keystore
keystore.properties
key.properties

# OTA bundles intermedios
ota-out/
*.zip

# Android signing keystore — NEVER commit
keystore.jks
*.jks
*.keystore
